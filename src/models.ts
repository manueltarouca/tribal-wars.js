export interface ClientConfig {
  type: string;
  world: number
  countryCode: string
}

export interface PlayerInterface {
  player_id: string;
  name: string;
  tribe_id: string;
  villages: string;
  points: string;
  rank: string;
}

// export interface PlayerProfile {

// }